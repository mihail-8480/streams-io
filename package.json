{
  "name": "@mojsoski/streams-io",
  "version": "1.0.0",
  "description": "NodeJS-like stream support for @mojsoski/streams",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "stream",
    "io"
  ],
  "author": "Mihail Mojsoski",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@mojsoski/streams": "^1.0.1"
  },
  "scripts": {
    "watch": "npx tsc --watch",
    "build": "npx tsc",
    "prepare": "npm run build",
    "version": "git add -A lib",
    "postversion": "git push && git push --tags"
  },
  "publishConfig": {
    "access": "public"
  }
}
